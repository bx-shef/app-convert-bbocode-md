# Domain — same nginx-proxy + acme-companion stack as the apps.
VIRTUAL_HOST=deploy-hook.bx-shef.by
LETSENCRYPT_HOST=deploy-hook.bx-shef.by
LETSENCRYPT_EMAIL=shevchik.mail@gmail.com

# Long random string. The same value goes into Settings → Secrets and variables
# → Actions → Secrets as `DEPLOY_HOOK_TOKEN`. Generate e.g. with:
#   openssl rand -hex 32
DEPLOY_HOOK_TOKEN=replace-me-with-a-long-random-string
