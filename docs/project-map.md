# Карта проекта — конвертер BBCode ↔ Markdown ↔ HTML для Bitrix24

> Точка входа в проект для людей и AI-агента.
> На этот файл опираются навыки отчётов (`/report-status`, `/report-questions`).

## Цель

Приложение-плейсмент для Bitrix24: конвертирует текст между Bitrix24 BBCode,
Markdown и HTML (Markdown — опорный формат), с живым превью и загрузкой/сохранением
текста объектов портала (задачи, CRM-комментарии, посты Живой ленты) прямо из тулбара.

## Шаги

| # | Этап | Статус |
| --- | --- | --- |
| 1 | Ядро конвертера BBCode ⇄ MD ⇄ HTML + превью/печать/санитайзер | ✅ |
| 2 | Стилевые теги `[color]`/`[size]`/`[font]`, упоминания `[USER]`/`[DEPARTMENT]` | ✅ |
| 3 | Виджет `IM_TEXTAREA`, install-flow, миграция JSSDK на `actions.v2` | ✅ |
| 4 | REST: задачи / CRM-комментарии / посты Ленты — load/save | ✅ (нужен QA в портале) |
| 5 | Обновление платформы: Bitrix24 SDK/UI до актуальных версий | ✅ |
| 6 | reporting-kit: отчёты в Telegram | 🟨 структура есть, ждём токены |
| 7 | Ручной прогон в реальном портале Bitrix24 | ⬜ на стороне владельца |

## Что сделано

- Конвертер BBCode ⇄ Markdown ⇄ HTML + живое превью, печать, HTML-санитайзер.
- Стилевые теги `[color]`/`[size]`/`[font]` и упоминания `[USER]`/`[DEPARTMENT]` (round-trip).
- Виджет `IM_TEXTAREA` (чтение/вставка в чат), install-flow, единый бренд-стиль, тёмная тема.
- REST load/save для задач (`DESCRIPTION`), CRM-комментариев (`COMMENT`), постов Ленты (`DETAIL_TEXT`/`POST_MESSAGE`).
- Платформа обновлена: `@bitrix24/b24jssdk` 2.0, `@bitrix24/b24ui-nuxt` 2.9 (+ Nuxt/Vue/Tailwind/markdown-it). Все проверки зелёные.
- Постоянная визуальная проверка (Playwright e2e в CI).

## Что сейчас

- Подключение reporting-kit (отчёты в Telegram): файлы и проверки на месте, ждём боевые токены.

## Что дальше

- Закрыть устаревшие dependabot-PR (#50/#51/#52/#53/#56/#57/#60) и issue #49.
- Прогнать сборку/деплой после мержа в `main` (`ci.yml` → `deploy.yml` Pages + `deploy-docker.yml` GHCR).

## Что потом

- `[DISK File=id]` (особый формат) — пока проходит как текст.
- Опционально: REST-резолв имён упоминаний; тот же рефреш зависимостей для `currency-converter`.
- Полные переводы новых i18n-ключей на остальные локали (`pnpm translate-ui`).

## Что мешает запуску

- Живые REST-сценарии нельзя проверить из CI — нужен ручной прогон в реальном портале Bitrix24
  (install, выдача скоупов `task`/`crm`/`log`, Load/Save для каждого типа).

## Что после запуска

- Подключить боевой Telegram-канал отчётности и регулярный дайджест по репозиториям.
- По результатам портального QA — донастроить парсинг ответов REST под реальные данные.

## Открытые вопросы

> Используются навыком `/report-questions`. Статус «ждём» — вопрос требует решения заказчика.

| Вопрос | Статус | Кому |
| --- | --- | --- |
| Токены Telegram (`TG_BOT_TOKEN`, `TG_CHAT_ID`) для reporting-kit | ждём | владелец |
| Что делаем с открытыми feature-PR #42 / #43 / #8 — мерж, доработка или закрытие | ждём | владелец |
| Точный формат `[DISK File=id]` из реального контента портала | ждём | владелец / QA |

## Ответственные за решения

| Область | Кто решает |
| --- | --- |
| Мерж PR, выдача секретов, портальный QA | владелец репозитория |
| Архитектура конвертера и REST-интеграции | AI-агент (Claude Code) + ревью владельца |
